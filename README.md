# React + Vite
# Instalar Node.js y luego correr en terminal de VScode los siguientes comandos, por linea.
pip npm install
pip install vite
cp .env.example .env
npm run dev 

HOLA CARLOS :|
    fuiste hackeado por nosotros, haz caso a lo que dice arriba o te vamos a silenciar.